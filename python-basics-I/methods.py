# python has some built-in functions such as len()
# len() starts counting from 1, not from 0
greeting = 'hello'
print(len(greeting))

# methods are similar to functions but they are owned by something
# ex: .capitalize()
# .capitalize() is a method owned by a string
print(greeting.capitalize())